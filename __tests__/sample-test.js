let React = require('react');
let expect = require('expect');

describe('sample test', function () {
  it('counts numbers', function () {
    let sampleInt = 1;
    expect(sampleInt).toEqual(1);
  });
  it('this test must be written');
});