'use strict';

const initialState = {
  loading: false,
  loaded: false,
};

export default function headerInfo(state = initialState, action) {
  switch (action.type) {

    default:
      return state;
  }
}
